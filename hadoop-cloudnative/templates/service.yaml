apiVersion: v1
kind: Service
metadata:
  name: {{ include "hadoop-cloudnative.fullname" . }}
  labels:
    {{- include "hadoop-cloudnative.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hadoop-cloudnative.selectorLabels" . | nindent 4 }}
